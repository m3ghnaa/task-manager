<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <nav class="bg-gray-800 shadow-md py-8 px-6">
        <div class="container mx-auto flex justify-between items-center">
            
            <!-- 🔹 Logo -->
            <a href="<%= user ? '/tasks' : '/' %>" class="text-xl text-gray-200 font-bold">
                Task Manager
            </a>
            
    
            <!-- 🔹 Navbar Links -->
            <div class="space-x-6">
                <% if (!user) { %>
                    <a href="/auth/login" class="text-gray-300 hover:text-gray-200 transition">Login</a>
                    <a href="/auth/signup" class="text-gray-300 hover:text-gray-200 transition">Signup</a>
                <% } else { %>
                    <a href="/tasks" class="text-gray-300 hover:text-gray-200 transition">Tasks</a>
                    <a href="/auth/logout" class="text-gray-300 hover:text-gray-200 transition" onclick="return confirmLogout()">Logout</a>
                <% } %>
            </div>
    
        </div>


    <script>
        function confirmLogout() {
            return confirm("Are you sure you want to log out?");
        }
    </script>
    </nav>
    

      
